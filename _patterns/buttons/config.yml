patterns:
  basic:
    fields:
      - name: url
        type: url
        example: "/case-studies/natures-care/"
      - name: text
        type: text
        example: "view project"

  glow:
    fields:
      - name: url
        type: url
        example: "mailto:hello@treawnaharvey.ca"
      - name: text
        type: text
        example: say hello

  btn-work:
    fields:
      - name: url
        type: url
        example: "/case-studies/mindu/"
      - name: text-color
        type: string
        example: "btn-mindu"
      - name: btn-work-shadow
        type: string
        example: "btn-mindu-shadow"
