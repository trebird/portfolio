patterns:
  basic:
    fields:
      - name: url
        type: url
        example: "/case-studies/natures-care/"
      - name: text
        type: text
        example: "view project"

  glow:
    fields:
      - name: url
        type: url
        example: "mailto:hello@treawnaharvey.ca"
      - name: text
        type: text
        example: say hello
